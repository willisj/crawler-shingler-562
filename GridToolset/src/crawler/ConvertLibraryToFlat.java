package crawler;

public class ConvertLibraryToFlat {

	public static void main(String[] args) {
		final String convertedPageFiles = "PATHTOFILES";

		/*
		 * ArrayList<File> pages = util .loadFiles("timhortons.com",
		 * Integer.MAX_VALUE);
		 * 
		 * HashMap<Integer, String> keys = new HashMap<Integer, String>(); int
		 * counter = 0; for (File path : pages) { try { File f = new
		 * File(convertedPageFiles + counter + ".html"); PageLW p =
		 * PageLW.load(path); if (p == null) { continue; }
		 * 
		 * FileUtils.writeStringToFile(f, p.source); keys.put(counter++, p.url);
		 * 
		 * } catch (IOException e) { // TODO Auto-generated catch block
		 * e.printStackTrace(); continue; } }
		 * 
		 * try { FileUtils.writeLines(new File(
		 * cfg.MasterConfig.pathConvertedPageFiles + "key.txt"), keys
		 * .entrySet()); } catch (IOException e) { // TODO Auto-generated catch
		 * block e.printStackTrace(); }
		 */
	}

}
